import { CeremoniesFilterPipe } from './ceremonies-filter.pipe';

describe('CeremoniesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CeremoniesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
